apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anair-services-helm.fullname" . }}-test-connection"
  labels:
    {{- include "anair-services-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "anair-services-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
